export type ItemDatabaseType = {
  id: string;
  title: string;
  model: string;
  quantity: number;
  department: string;
  category: string;
  description: string;
}

export type CategoriesType = {
  title: string;
  color: string;
}
